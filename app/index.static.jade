doctype html
html(lang='ru')
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")

    title Code Hipsters

    link(href='http://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700|Roboto:400,100,300,500,700,900&subset=latin,cyrillic,cyrillic-ext' rel='stylesheet' type='text/css')

    //- Следующие файлы генерируются сборщиком Brunch
    link(rel="stylesheet" href="stylesheets/app.css")

    script(src="javascripts/vendor.js")
    script(src="javascripts/app.js")

    script.
      require('scripts/initialize');

    //- ---
    //- Клиентские шаблоны
    //- ---
    script#message(type='text/template')
      .message.min-height-message
        .message-icon-wrapper 
          .message-icon
            img(src='message-icon.png', alt='Smiley face', height='34', width='34')
        .message-body-wrapper.min-height-message-body-wrapper
          .message-body
            .message-header <%= header %>
            .message-content <%= message %>

  body
    .messages-container

    .speed-indicator
      .left-background
        .slow-bar
      .right-background
        .fast-bar
      .middle.show

    .reveal
      .slides
        section(data-slide-name='1')
          h3 Кто самый крутой?
          #poll

        section(data-slide-name='2')
          h1 Слайд 2

        section(data-slide-name='3')
          h3 Подзаголовок
          p привет из слайда 2

        section(data-slide-name='4')
          h3 Слайд с чуваками

          button(onclick="drawHumans($('.human').length + 1);")
          button(onclick="drawHumans($('.human').length - 1);")

          #participants-container
            .meter
              .count-next 0
              .count 0

            .people


